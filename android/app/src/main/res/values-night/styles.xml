<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is on -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             Flutter draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>\
    </style>

    <!-- SalesIQ theme -->
    <style name="Theme.SalesIQ.Base" parent="Theme.SalesIQ.Light.DarkActionBar">
        <item name="colorAccent">@color/colorAccent</item>
        <item name="siq_statusbar_color">@color/colorDarkSecondary</item>
        <item name="siq_toolbar_backgroundcolor">@color/colorSecondary</item>
        <item name="siq_toolbar_title_textcolor">@color/colorPrimary</item>
        <item name="siq_toolbar_iconcolor">@color/colorPrimary</item>

        <item name="siq_launcher_backgroundcolor">@color/colorPrimary</item>

        <item name="siq_forms_statusbar_color">@color/colorDarkSecondary</item>
        <item name="siq_forms_toolbar_backgroundcolor">@color/colorSecondary</item>
        <item name="siq_forms_submitbutton_backgroundcolor">@color/colorPrimary</item>
        <item name="siq_forms_submitbutton_textcolor">@android:color/white</item>

        <item name="siq_chat_message_backgroundcolor_visitor">@color/colorPrimary</item>

        <item name="siq_emptyview_textcolor">@color/colorPrimary</item>
        <item name="siq_emptyview_button_backgroundcolor">@color/colorPrimary</item>
        <item name="siq_emptyview_button_textcolor">@android:color/white</item>
        <item name="siq_emptyview_button_iconcolor">@android:color/white</item>
    </style>

</resources>
